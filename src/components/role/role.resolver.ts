import { Resolver } from '@nestjs/graphql';

@Resolver()
export class RoleResolver {
  //   @Mutation(() => [Role])
  //   async addRoles(@Args('input') roles: string[]) {
  //     return this._userService.addRoles(roles);
  //   }
}
